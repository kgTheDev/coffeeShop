<!doctype html>
<html>
<head>
    <title>Barista</title>
    <link rel="stylesheet" href="//netdna.bootstrapcdn.com/bootstrap/3.0.2/css/bootstrap.min.css">
    <link rel="stylesheet" href="//netdna.bootstrapcdn.com/font-awesome/4.0.3/css/font-awesome.min.css">
    <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.1.0/css/all.css" integrity="sha384-lKuwvrZot6UHsBSfcMvOkWwlCMgc0TaWr+30HWe3a4ltaBwTZhyTEggF5tJv8tbt" crossorigin="anonymous">
    <link rel="stylesheet" href="style.css">
</head>
<body>
<div class="container">

    <div class="page-header text-center">
        <h1><span class="fa fa-anchor"></span>Welcome Barista: <%= user.local.email %> </h1>
        <a href="/logout" class="btn btn-default btn-sm">Logout</a>
    </div>

    <div class="row">

        <!-- LOCAL INFORMATION -->
        <div class="col-sm-6">
            <div class="well">
                <h3><span class="fa fa-user"></span> Local</h3>

                <% if (user.local.email) { %>
                    <p>
                        <strong>id</strong>: <%= user._id %><br>
                        <strong>email</strong>: <%= user.local.email %><br>
                        <!-- <strong>password</strong>: <%= user.local.password %> -->
                    </p>

                    <a href="/unlink/local" class="unlink">Unlink</a>
                <% } else { %>
                    <a href="/connect/local" class="btn btn-default">Connect Local</a>
                <% } %>

            </div>
        </div>
        <div class="col-sm-5">
          <h3>Pending Orders</h3>
          <ol class="completed">
          <% for(var i=0; i<completed.length; i++) {%>
            <li class="completed">
              <span><%= completed[i].name %></span>
              <span><%= completed[i].customer %></span>
              <span><%= completed[i].coffee %></span>
              <span><%= completed[i].size %></span>
              <span><%= completed[i].check %></span>
              <span><%= completed[i].thumbDown %></span>
              <span><%= completed[i].ban %></span>
              <span><i class="fas fa-check" aria-hidden="true"></i></span>
              <!-- <span><i class="fa fa-thumbs-down" aria-hidden="true"></i></span> -->
              <span><i class="fas fa-ban" aria-hidden="true"></i></span>
            </li>
          <% } %>
          </ol>


          <h2></h2>
        <% if (user.local.email) { %>
          <form action="/completed" method="POST">
            <!-- <input type="text" value="<%= user.local.email %>" name="name"> -->
            <!-- <input type="text" value="" name="customer"> -->
            <!-- <select class="coffee" name="coffee">
              <option value="Dark Roast">Dark Roast</option>
              <option value="Cappuccino">Cappuccino</option>
              <option value="Flat White">Flat White</option>
              <option value="Macchiato">Macchiato</option>
              <option value="Regular">Regular</option>
              <option value="Latte">Latte</option>
              <option value="Americano">Americano</option>
            </select>
            <select class="size" name="size">
              <option value="Small">small</option>
              <option value="Medium">Medium</option>
              <option value="Large">Large</option>
              <option value="Extra Large">Extra Large</option>
            </select>
            <button type="submit">Order</button> -->
          </form>
        </div>
        <% } %>

</div>

<script src="main.js"></script>

</body>
</html>
